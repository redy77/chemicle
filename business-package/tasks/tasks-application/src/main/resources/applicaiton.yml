spring:
  application:
    name: tasks-application

server:
  port: 0
  servlet:
    context-path: /tasks-application/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
